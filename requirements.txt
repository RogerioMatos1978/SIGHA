Django>=5.0,<5.2
djangorestframework>=3.15
psycopg2-binary>=2.9
python-decouple>=3.8
django-ratelimit>=4.1
redis>=5.0
gunicorn>=22.0
whitenoise>=6.7
ortools>=9.10        # Módulo 11 — motor de geração automática de grade (CP-SAT)
openpyxl>=3.1        # Módulo 14 — exportação da grade em Excel
reportlab>=4.2       # Módulo 14 — exportação da grade em PDF
python-docx>=1.1     # Módulo 14 — exportação da grade em Word
Pillow>=10.4         # Módulo 14 — exportação da grade em PNG/JPEG
coverage>=7.6        # Módulo 18 — relatório de cobertura dos testes
