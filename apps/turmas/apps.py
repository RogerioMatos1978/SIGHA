from django.apps import AppConfig


class TurmasConfig(AppConfig):
    """Configuração do app Turmas — Módulo 6 do SIGHA."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.turmas'
    label = 'turmas'
    verbose_name = 'Turmas'
