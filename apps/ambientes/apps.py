from django.apps import AppConfig


class AmbientesConfig(AppConfig):
    """Configuração do app Ambientes — Módulo 7 do SIGHA."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.ambientes'
    label = 'ambientes'
    verbose_name = 'Ambientes'
