from django.apps import AppConfig


class HorariosConfig(AppConfig):
    """Configuração do app Horários — Módulo 8 do SIGHA."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.horarios'
    label = 'horarios'
    verbose_name = 'Horários'
