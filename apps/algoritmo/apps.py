from django.apps import AppConfig


class AlgoritmoConfig(AppConfig):
    """Configuração do app Algoritmo automático — Módulo 11 do SIGHA."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.algoritmo'
    label = 'algoritmo'
    verbose_name = 'Algoritmo automático'
